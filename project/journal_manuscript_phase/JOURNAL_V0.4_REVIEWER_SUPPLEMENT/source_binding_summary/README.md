# Source and deployment interpretation

Public lineage is bounded to inspected repositories and histories. Repository ancestry alone is not deployed-variant identity. The Vesta implementation summary records PriceFeedV2 at the executed proxy, including the absence of the assumed index-age predicate; the two earlier reference-binding cases remain non-comparable. Aurigami's bound nonproxy root and code digest are in the identity matrix. Fathom correspondence uses the deployment-era commit and executable bytes, not full metadata equality. These are source/binding summaries, not a replacement for all historical qualification logs.

The public source URLs/commits and source hashes are retained in public_lineage.csv. Third-party project authorship is preserved; it is not study-author identifying metadata. The Fathom source/runtime reports state the exact deployment-era compiler and dependency correspondence. No network query is needed to read the included summaries.
