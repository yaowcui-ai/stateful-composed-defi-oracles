# Deployment identities

Read stateful_selection.json for all three chain/block/hash/root anchors. The separate stateful_identity_matrix.csv supplies Aurigami/Fathom code digests, and vesta_implementation.json identifies executed PriceFeedV2. Fathom executable correspondence and the dependency binding are in ../source_binding_summary. numeric_selection.json identifies the six selected numeric roots and formulas. Historical source labels in projections are not new runtime checks.
